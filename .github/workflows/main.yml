name: Ansible Molecule
on: 
  push:
    branches: 
      - main 

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
    steps:
      - name: Test with molecule
        run: |
          molecule converge && molecule verify
